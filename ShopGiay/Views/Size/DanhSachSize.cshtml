@model dynamic
<div class="mb-4">
    <h3 class="mb-3 h6 text-uppercase text-black d-block" style="        color: black;
        font-weight: 600;
        font-size: 20px
">Size</h3>
    <ul>
        @foreach (var item in Model)
        {
            <li><a href="#" class="d-flex text-black"><span>@item.Size</span></a></li>
        }
    </ul>
</div>

